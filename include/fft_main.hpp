#ifndef FFT_MAIN_HPP
#define FFT_MAIN_HPP

/**
 * @file fft_main.hpp.
 * @brief Declares the main function for CPU FFT-related tests.
 */

/**
 * @brief The main function for CPU FFT-related tests.
 *
 * The function is called from main when the first argument is fft.
 */
int fft_main(int argc, char *argv[]);

#endif  // FFT_MAIN_HPP