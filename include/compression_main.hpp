#ifndef JPEG_MAIN_HPP
#define JPEG_MAIN_HPP

/**
 * @file compression_main.hpp.
 * @brief Declares the main function for compression-related tests.
 */

/**
 * @brief The main function for compression-related tests.
 *
 * The function is called from main when the first argument is compression.
 */
int compression_main(int argc, char *argv[]);

#endif  // JPEG_MAIN_HPP